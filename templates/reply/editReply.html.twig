{% extends 'base.html.twig' %}

{% block title %}Modifier la réponse{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center mb-4">Modifier la réponse</h1>
        <div class="row justify-content-center">
            <div class="col-md-6">
                {{ form_start(form) }}
                <div class="form-group">
                    {{ form_label(form.responseDate, 'Date de Réponse') }}
                    {{ form_widget(form.responseDate, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="form-group">
                    {{ form_label(form.responseContent, 'Contenu') }}
                    {{ form_widget(form.responseContent, {'attr': {'class': 'form-control'}}) }}
                </div>
                
                <div class="form-group">
                    {{ form_label(form.reclamation, 'Réclamation') }}
                    {{ form_widget(form.reclamation, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="text-center">
                    <button type="submit" class="btn btn-primary">Enregistrer les modifications</button>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}